import { GuestFilterPipe } from './guest-filter.pipe';

describe('GuestFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GuestFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
